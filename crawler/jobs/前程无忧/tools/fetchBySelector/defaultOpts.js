const defaultOpts = {
    selector: 'a',
    attr: 'text',
    trim: true,
    deDuplication: true,
    handler: null,
    test: null,
    process: null,
    filter:null
};
module.exports = defaultOpts;